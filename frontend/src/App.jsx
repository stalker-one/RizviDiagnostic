import React, { useEffect, useRef, useState } from 'react';
import { Routes, Route, Navigate, useLocation, useNavigate } from 'react-router-dom';
import { Capacitor, registerPlugin } from '@capacitor/core';
import ProtectedRoute from './components/ProtectedRoute.jsx';
import SiteLockGate from './components/SiteLockGate.jsx';
import api from './api/axios';
import Login from './pages/Login.jsx';
import AdminLogin from './pages/AdminLogin.jsx';
import Home from './pages/Home.jsx';
import Dashboard from './pages/Dashboard.jsx';
import Patients from './pages/Patients.jsx';
import PatientDetail from './pages/PatientDetail.jsx';
import CreateInvoice from './pages/CreateInvoice.jsx';
import Invoices from './pages/Invoices.jsx';
import InvoicePrint from './pages/InvoicePrint.jsx';
import RadiologyReports from './pages/RadiologyReports.jsx';
import Analytics from './pages/Analytics.jsx';
import Referrals from './pages/Referrals.jsx';
import Doctors from './pages/Doctors.jsx';
import Procedures from './pages/Procedures.jsx';
import Users from './pages/Users.jsx';
import Settings from './pages/Settings.jsx';
import Profile from './pages/Profile.jsx';
import SiteControl from './pages/SiteControl.jsx';

const REALTIME_INTERVAL_MS = 1500;
const ANDROID_UPDATE_INTERVAL_MS = 5 * 60 * 1000;
const AndroidUpdate = registerPlugin('AndroidUpdate');
const IS_SUPERADMIN_APP = import.meta.env.VITE_SUPERADMIN_APP === 'true';

function SuperadminGuard(){const location=useLocation();const navigate=useNavigate();const token=localStorage.getItem('rdc_token');useEffect(()=>{if(!IS_SUPERADMIN_APP)return;if(!token&&location.pathname!=='/adminlogin')navigate('/adminlogin',{replace:true});},[location.pathname,navigate,token]);return null;}

function formatBytes(bytes){if(!Number.isFinite(bytes)||bytes<=0)return '';if(bytes<1024*1024)return `${(bytes/1024).toFixed(0)} KB`;return `${(bytes/1024/1024).toFixed(1)} MB`;}
function AndroidUpdateModal({update,checking,onUpdate,busy,error,onRetry,progress}){if(!checking&&!update&&!error)return null;const appName=IS_SUPERADMIN_APP?'Rizvi Diagnostic Center Superadmin':'Rizvi Diagnostic Center';const percent=Math.max(0,Math.min(100,Number(progress?.percent||0)));return <div style={{position:'fixed',inset:0,zIndex:999999,display:'flex',alignItems:'center',justifyContent:'center',background:'rgba(0,0,0,.78)',padding:16}}><div style={{width:'100%',maxWidth:460,maxHeight:'92vh',overflowY:'auto',borderRadius:18,background:'#fff',padding:22,boxShadow:'0 20px 60px rgba(0,0,0,.35)'}}>{checking?<><h2 style={{margin:'0 0 8px',fontSize:22,fontWeight:700}}>Checking for updates…</h2><p style={{margin:0,color:'#555'}}>Checking the official {appName} release.</p></>:update?<><h2 style={{margin:'0 0 8px',fontSize:22,fontWeight:700}}>Update required</h2><p style={{margin:'0 0 14px',color:'#555'}}>A newer version of {appName} is available.</p><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:10,marginBottom:16}}><div style={{padding:12,borderRadius:10,background:'#f3f4f6'}}><div style={{fontSize:12,color:'#666'}}>Current version</div><div style={{fontWeight:700,fontSize:16}}>{update.installedVersionName||'Unknown'}</div><div style={{fontSize:12,color:'#666'}}>Build {update.installedVersionCode??'—'}</div></div><div style={{padding:12,borderRadius:10,background:'#eef6ff'}}><div style={{fontSize:12,color:'#666'}}>New version</div><div style={{fontWeight:700,fontSize:16}}>{update.versionName}</div><div style={{fontSize:12,color:'#666'}}>Build {update.versionCode}</div></div></div><div style={{marginBottom:8,fontWeight:700}}>Download size</div><div style={{margin:'0 0 16px',padding:10,borderRadius:10,background:'#f8fafc',fontSize:14}}>{formatBytes(update.sizeBytes)||`${update.sizeMB||'—'} MB`}</div><div style={{marginBottom:8,fontWeight:700}}>What’s new</div><div style={{margin:'0 0 16px',padding:12,borderRadius:10,background:'#f3f4f6',fontSize:14,whiteSpace:'pre-wrap',maxHeight:210,overflowY:'auto'}}>{update.releaseNotes||'Latest Android application update with bug fixes and improvements.'}</div>{busy&&<div style={{margin:'0 0 16px'}}><div style={{display:'flex',justifyContent:'space-between',fontSize:13,color:'#555',marginBottom:6}}><span>Downloading update…</span><strong>{percent}%</strong></div><div style={{height:10,width:'100%',background:'#e5e7eb',borderRadius:999,overflow:'hidden'}}><div style={{height:'100%',width:`${percent}%`,background:'#111827',transition:'width .2s ease'}}/></div><div style={{fontSize:12,color:'#666',marginTop:6}}>{formatBytes(progress?.downloadedBytes)}{progress?.totalBytes>0?` / ${formatBytes(progress.totalBytes)}`:''}</div></div>}{error&&<p style={{margin:'0 0 12px',color:'#b91c1c',fontSize:14,wordBreak:'break-word'}}>{error}</p>}<button type="button" onClick={onUpdate} disabled={busy} style={{width:'100%',border:0,borderRadius:10,padding:'13px 16px',background:'#111827',color:'#fff',fontWeight:700}}>{busy?`Downloading ${percent}%…`:'Update now'}</button></>:<><h2 style={{margin:'0 0 8px',fontSize:22,fontWeight:700}}>Update check failed</h2><p style={{margin:'0 0 16px',color:'#555'}}>The application could not verify the latest Android release.</p><p style={{margin:'0 0 16px',color:'#b91c1c',fontSize:13,wordBreak:'break-word'}}>{error}</p><button type="button" onClick={onRetry} style={{width:'100%',border:0,borderRadius:10,padding:'12px 16px',background:'#111827',color:'#fff',fontWeight:700}}>Check again</button></>}</div></div>}

// Staff Android must never return an already-authenticated user to the public
// Home page when Android recreates the WebView/activity. If a valid persisted
// session token exists, the root route immediately resumes at Dashboard.
function StaffEntryRoute(){
  const token = localStorage.getItem('rdc_token');
  if (token) return <Navigate to="/dashboard" replace />;
  return <Home />;
}

export default function App(){const[refreshKey,setRefreshKey]=useState(0);const[androidUpdate,setAndroidUpdate]=useState(null);const[androidUpdateBusy,setAndroidUpdateBusy]=useState(false);const[androidUpdateError,setAndroidUpdateError]=useState('');const[androidUpdateChecking,setAndroidUpdateChecking]=useState(false);const[androidUpdateProgress,setAndroidUpdateProgress]=useState({percent:0,downloadedBytes:0,totalBytes:0});const checkingAndroidUpdate=useRef(false);const retryCheckRef=useRef(null);const lastVersionRef=useRef(null);
useEffect(()=>{let stopped=false;const checkVersion=async()=>{if(stopped)return;try{const response=await api.get('/sync/version',{params:{_:Date.now()},headers:{'Cache-Control':'no-cache'}});const version=Number(response.data?.version||0);if(lastVersionRef.current!==null&&version!==lastVersionRef.current)setRefreshKey(v=>v+1);lastVersionRef.current=version;}catch{}};checkVersion();const timer=window.setInterval(checkVersion,REALTIME_INTERVAL_MS);return()=>{stopped=true;window.clearInterval(timer);};},[]);
useEffect(()=>{if(!Capacitor.isNativePlatform()||Capacitor.getPlatform()!=='android')return undefined;let cancelled=false;let progressListener;const checkAndroidUpdate=async()=>{if(cancelled||checkingAndroidUpdate.current)return;checkingAndroidUpdate.current=true;setAndroidUpdateChecking(true);setAndroidUpdateError('');try{if(typeof AndroidUpdate.checkForUpdate!=='function')throw new Error('AndroidUpdate native plugin is not registered in this APK. Install the newly built APK.');const result=await AndroidUpdate.checkForUpdate();if(cancelled)return;if(result?.available&&result?.url){setAndroidUpdate({versionCode:Number(result.versionCode),versionName:result.versionName||`1.0.${Math.max(0,Number(result.versionCode)-1)}`,url:result.url,packageName:result.packageName,releaseName:result.releaseName,releaseNotes:result.releaseNotes||'',commit:result.commit||'',installedVersionCode:Number(result.installedVersionCode),installedVersionName:result.installedVersionName||'',sizeBytes:Number(result.sizeBytes||0),sizeMB:Number(result.sizeMB||0)});}else setAndroidUpdate(null);}catch(error){if(!cancelled)setAndroidUpdateError(error?.message||String(error)||'Unable to check the Android release.');}finally{checkingAndroidUpdate.current=false;if(!cancelled)setAndroidUpdateChecking(false);}};retryCheckRef.current=checkAndroidUpdate;AndroidUpdate.addListener('updateProgress',(event)=>{if(!cancelled)setAndroidUpdateProgress(event||{percent:0});}).then(listener=>{progressListener=listener;});checkAndroidUpdate();const onResume=()=>{if(document.visibilityState==='visible')checkAndroidUpdate();};document.addEventListener('visibilitychange',onResume);window.addEventListener('focus',onResume);const timer=window.setInterval(checkAndroidUpdate,ANDROID_UPDATE_INTERVAL_MS);return()=>{cancelled=true;window.clearInterval(timer);document.removeEventListener('visibilitychange',onResume);window.removeEventListener('focus',onResume);retryCheckRef.current=null;progressListener?.remove?.();};},[]);
const installAndroidUpdate=async()=>{if(!androidUpdate?.url||androidUpdateBusy)return;setAndroidUpdateBusy(true);setAndroidUpdateError('');setAndroidUpdateProgress({percent:0,downloadedBytes:0,totalBytes:androidUpdate.sizeBytes||0});try{await AndroidUpdate.installApk({url:androidUpdate.url});}catch(error){setAndroidUpdateError(error?.message||'Unable to start the Android update. Please allow this app to install updates and try again.');}finally{setAndroidUpdateBusy(false);}};
const protectedRoutes=<><Route path="/dashboard" element={<ProtectedRoute><Dashboard/></ProtectedRoute>}/><Route path="/patients" element={<ProtectedRoute><Patients/></ProtectedRoute>}/><Route path="/patients/:id" element={<ProtectedRoute><PatientDetail/></ProtectedRoute>}/><Route path="/invoices/create" element={<ProtectedRoute><CreateInvoice/></ProtectedRoute>}/><Route path="/invoices" element={<ProtectedRoute><Invoices/></ProtectedRoute>}/><Route path="/invoices/:id/print" element={<ProtectedRoute><InvoicePrint/></ProtectedRoute>}/><Route path="/radiology-reports" element={<ProtectedRoute><RadiologyReports/></ProtectedRoute>}/><Route path="/analytics" element={<ProtectedRoute><Analytics/></ProtectedRoute>}/><Route path="/referrals" element={<ProtectedRoute><Referrals/></ProtectedRoute>}/><Route path="/doctors" element={<ProtectedRoute><Doctors/></ProtectedRoute>}/><Route path="/procedures" element={<ProtectedRoute><Procedures/></ProtectedRoute>}/><Route path="/users" element={<ProtectedRoute adminOnly><Users/></ProtectedRoute>}/><Route path="/settings" element={<ProtectedRoute adminOnly><Settings/></ProtectedRoute>}/><Route path="/site-control" element={<ProtectedRoute superadminOnly><SiteControl/></ProtectedRoute>}/><Route path="/profile" element={<ProtectedRoute><Profile/></ProtectedRoute>}/></>;const routes=IS_SUPERADMIN_APP?<Routes><Route path="/adminlogin" element={<AdminLogin/>}/>{protectedRoutes}<Route path="*" element={<Navigate to="/dashboard" replace/>}/></Routes>:<Routes><Route path="/" element={<StaffEntryRoute/>}/><Route path="/login" element={<Login/>}/><Route path="/adminlogin" element={<AdminLogin/>}/>{protectedRoutes}<Route path="*" element={<Navigate to="/" replace/>}/></Routes>;return <><SiteLockGate/>{IS_SUPERADMIN_APP&&<SuperadminGuard/>}<div key={refreshKey} className="contents">{routes}</div><AndroidUpdateModal update={androidUpdate} checking={androidUpdateChecking} busy={androidUpdateBusy} error={androidUpdateError} progress={androidUpdateProgress} onUpdate={installAndroidUpdate} onRetry={()=>retryCheckRef.current?.()}/></>;}
